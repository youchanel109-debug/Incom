const money = value => new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(value || 0)
export default function SummaryCard({ summary }) { const data = [{ label: 'Total Income', value: summary.total_income, color: 'text-emerald-600' }, { label: 'Total Expenses', value: summary.total_expenses, color: 'text-blue-600' }, { label: 'Savings', value: summary.total_savings, color: 'text-amber-600' }, { label: 'NET', value: summary.net, color: summary.net >= 0 ? 'text-indigo-700' : 'text-rose-600' }]; return <section className="surface-card animate-slideUp rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-4 font-bold text-slate-800">Budget Summary</h2><div className="grid grid-cols-2 gap-4"><>{data.map((item, index) => <div key={item.label} className="rounded-2xl border border-slate-100 bg-slate-50/80 p-4 transition-all duration-300 hover:-translate-y-1 hover:shadow-lg animate-slideUp" style={{ animationDelay: `${index * 0.07}s` }}><p className="text-xs font-medium uppercase text-slate-400">{item.label}</p><p className={`mt-1 text-xl font-bold ${item.color}`}>{money(item.value)}</p></div>)}</></div></section> }

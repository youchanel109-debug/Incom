import { PieChart, Pie, Cell, Tooltip, ResponsiveContainer, Legend } from 'recharts'
const COLORS = ['#3b82f6','#6366f1','#8b5cf6','#06b6d4','#f59e0b','#ef4444']
export default function ExpensesDonutChart({ items }) { const data = items.filter(i => Number(i.actual)).map(i => ({ name: i.category_name, value: Number(i.actual) })); return <section className="surface-card rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-800">Expenses Breakdown</h2><div className="mt-3 h-48">{data.length ? <ResponsiveContainer><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={48} outerRadius={75} animationDuration={700}>{data.map((_, i) => <Cell key={i} fill={COLORS[i % COLORS.length]}/>)}</Pie><Tooltip formatter={v => `$${Number(v).toLocaleString()}`}/><Legend/></PieChart></ResponsiveContainer> : <div className="grid h-full place-items-center text-sm text-slate-400">Add expenses to see breakdown</div>}</div></section> }

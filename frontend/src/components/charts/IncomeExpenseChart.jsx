import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, Legend } from 'recharts'
export default function IncomeExpenseChart({ summary }) { const data = [{ name: 'Budget', Income: Number(summary.income_budgeted || 0), Expenses: Number(summary.expenses_budgeted || 0) }, { name: 'Actual', Income: Number(summary.total_income || 0), Expenses: Number(summary.total_expenses || 0) }]; return <section className="surface-card rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-800">Income vs Expenses</h2><div className="mt-3 h-48"><ResponsiveContainer><BarChart data={data}><XAxis dataKey="name" tickLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip formatter={v => `$${Number(v).toLocaleString()}`}/><Legend/><Bar dataKey="Income" fill="#10b981" radius={[4,4,0,0]} animationDuration={650}/><Bar dataKey="Expenses" fill="#3b82f6" radius={[4,4,0,0]} animationDuration={650}/></BarChart></ResponsiveContainer></div></section> }
